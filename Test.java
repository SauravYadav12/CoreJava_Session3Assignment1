public class Test{
	
	static{
		print(10);
	}
	static void print (int x) {
		System.out.println (x);
		System.exit(0);
	}
	
	
	// The above code will not throw an error at compile time.
// But at runtime it will throw an error 

public static void main(String[] args)
	{						  // to make it run properly we need to define a main function.
							 
	}
}
